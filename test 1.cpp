#include<stdio.h>
#include<conio.h>
 
{

printf[enter the num ];
scanf(%d,&num);
}
// TO identify the number using recursion
printf[find the factorial and gcd];
scanf(%d,num);
if(i=1,i,j,j=2)
if(i==1) (j==2)
}
return 0
}
